# Expo setup guide (Windows)
1. This guide uses npm. It comes with node which the project needs. Install it here:
https://nodejs.org/en/. Make sure to install the LTS version.
2. npm i python3 (you can skip this if you already have python3)
In the root folder of the app run “npm install” to install all dependencies.
4. npx expo start
