# Expo setup guide (Linux)

1. This guide uses npm. You can install it with “npm install node” OR install it from
the official website: https://nodejs.org/en/. Make sure to install the LTS version.
2. Clone gitlab repository Login App.
3. npm install watchman (for better performance, can do without).
4. npm install (installs all dependencies for the project)
5. npm start
